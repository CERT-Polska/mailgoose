Configuration options
=====================

Mailgoose can be configured by setting the following variables in the ``.env`` file (in the form of ``VARIABLE_NAME=VARIABLE_VALUE``
directives, e.g. ``APP_DOMAIN=example.com``):

.. include:: config-docs.inc
